[project]
name = "crawler-project"
version = "0.1.0"
description = "Recursive crawler exposed through FastAPI for Vercel."
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4",
    "fastapi",
    "playwright",
    "tldextract",
    "uvicorn",
]
