{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "PLAYWRIGHT_BROWSERS_PATH=0 python -m playwright install --with-deps --only-shell chromium",
  "functions": {
    "app.py": {
      "maxDuration": 60,
      "excludeFiles": "{tests/**,venv/**,.tldextract-cache/**,__pycache__/**}"
    }
  }
}
